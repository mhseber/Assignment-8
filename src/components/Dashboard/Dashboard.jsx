import { Helmet } from "react-helmet";

const Dashboard = () => {
    return (
        <div>
            <Helmet>
                <title>Dashboard || Gadget Heaven</title>
            </Helmet>
            My dashboard
        </div>
    );
};

export default Dashboard;